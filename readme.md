# ytyz transcriber

todo:
1. self host
2. implement ejs for file cards
3. delete and edit transcription frontend
4. crud frontend on users for admins?
5. file status update to reflect on the progress bar instead of on the side (with string enum)
6. feedback to user on errors
7. api usage per endpoint for admins only
8. refactor frontend
9. refactor app.py to have real contstant per corey's suggestion
10. refactor transcription.py into a real library/module (with _init__) and all other corey's suggestions

stretch goals:
1. rust ui
2. cli
3. ts
4. email verification + password change via email + captcha on register